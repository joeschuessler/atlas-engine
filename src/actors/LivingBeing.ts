/*import { Entity } from "../simulation/Entity";

export abstract class LivingBeing extends Entity {
  age: number;

  constructor(name: string, age: number = 0) {
    super(name);
    this.age = age;
  }

  abstract act(): void;
}*/